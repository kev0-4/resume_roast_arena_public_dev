// fastapi, dotenv,  psycopg2, redis,azure-storage-blob , asyncio, sqlalchemy alembic,zon,firebase-admin, asyncpg,redis PyJWT,azure-storage-blob,  azure-servicebus azure-identity aiohttp , tika, spacy, libphonenumber, google-genai, playwright, jinja2
// NOTE: this file is an informal package list, not a real pip requirements file.
// The actual installable one (used by CI, `pip install -r requirements.txt`) is at the repo root.